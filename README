This was created for HackUTD19 by Ben Rust, Cameron Sheffield, Connor Hulla, Zhe An Qian 


For the Alexa skill, that's all in the Archive.zip (hopefully)

For the scrape.py, that's a python file that must have the bs4 package on the system, so run 
"pip3 install bs4" in the command line.  

Run "python setup.py install" in resulting directory from the download 

Use Python 3.6 

UTD Whoosh
